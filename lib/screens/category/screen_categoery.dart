import 'package:flutter/material.dart';

class screenCategory extends StatelessWidget {
  const screenCategory({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('screen cateory'));
  }
}